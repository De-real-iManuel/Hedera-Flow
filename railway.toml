[build]
builder = "nixpacks"

[deploy]
startCommand = "cd backend && uvicorn app.core.app:app --host 0.0.0.0 --port $PORT --workers 1"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[env]
PORT = "8000"
PYTHONPATH = "/app/backend"