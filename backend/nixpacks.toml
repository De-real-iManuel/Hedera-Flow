[phases.setup]
nixPkgs = ['openjdk11', 'gcc', 'g++']

[phases.install]
cmds = ['pip install -r requirements.txt']

[phases.build]
cmds = []

[start]
cmd = 'uvicorn app.core.app:app --host 0.0.0.0 --port $PORT'

[variables]
JAVA_HOME = '/nix/store/*-openjdk-11*/lib/openjdk'