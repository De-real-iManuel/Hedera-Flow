[pytest]
# Only collect tests from the tests/ directory to avoid importing scripts
# that require external services (JVM, live database, etc.)
testpaths = tests

# Register custom markers
markers =
    integration: mark test as requiring a live PostgreSQL database; skipped when DATABASE_URL points to SQLite

# Asyncio mode for pytest-asyncio
asyncio_mode = auto
