# Environment Variables for Railway Dashboard

# Copy these to your Railway service environment variables:

# Database (Railway will auto-provide when you connect PostgreSQL)
DATABASE_URL=${{Postgres.DATABASE_URL}}

# Redis (Railway will auto-provide when you connect Redis)  
REDIS_URL=${{Redis.REDIS_URL}}

# Hedera Configuration (Replace with your actual values)
HEDERA_ACCOUNT_ID=0.0.xxxxx
HEDERA_PRIVATE_KEY=your_private_key_here
HEDERA_NETWORK=testnet

# Authentication (Generate a secure 32+ character secret)
JWT_SECRET=your_super_secure_jwt_secret_minimum_32_characters_long
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Google Cloud (Base64 encode your JSON credentials file)
GOOGLE_CLOUD_CREDENTIALS=base64_encoded_json_credentials

# CORS (Update with your actual frontend domain)
ALLOWED_ORIGINS=https://your-app.vercel.app,http://localhost:3000

# Rate Limiting
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW=60

# Logging
LOG_LEVEL=INFO
ENVIRONMENT=production

# Port (Railway sets this automatically)
PORT=8000

# Python Path
PYTHONPATH=/app